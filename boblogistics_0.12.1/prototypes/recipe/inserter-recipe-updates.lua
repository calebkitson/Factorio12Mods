if data.raw.item["electronic-circuit-board"] then
  table.insert(data.raw["recipe"]["purple-inserter"].ingredients,{"electronic-circuit-board", 2})
  if data.raw.recipe["purple-near-inserter"] then
    table.insert(data.raw["recipe"]["purple-near-inserter"].ingredients,{"electronic-circuit-board", 2})
  end
  if data.raw.recipe["purple-far-inserter"] then
    table.insert(data.raw["recipe"]["purple-far-inserter"].ingredients,{"electronic-circuit-board", 2})
  end
  if data.raw.recipe["purple-long-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-inserter"].ingredients,{"electronic-circuit-board", 2})
  end
  if data.raw.recipe["purple-short-far-inserter"] then
    table.insert(data.raw["recipe"]["purple-short-far-inserter"].ingredients,{"electronic-circuit-board", 2})
  end
  if data.raw.recipe["purple-short-long-inserter"] then
    table.insert(data.raw["recipe"]["purple-short-long-inserter"].ingredients,{"electronic-circuit-board", 2})
  end
  if data.raw.recipe["purple-long-near-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-near-inserter"].ingredients,{"electronic-circuit-board", 2})
  end
  if data.raw.recipe["purple-long-short-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-short-inserter"].ingredients,{"electronic-circuit-board", 2})
  end
else
  table.insert(data.raw["recipe"]["purple-inserter"].ingredients,{"advanced-circuit", 2})
  if data.raw.recipe["purple-near-inserter"] then
    table.insert(data.raw["recipe"]["purple-near-inserter"].ingredients,{"advanced-circuit", 2})
  end
  if data.raw.recipe["purple-far-inserter"] then
    table.insert(data.raw["recipe"]["purple-far-inserter"].ingredients,{"advanced-circuit", 2})
  end
  if data.raw.recipe["purple-long-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-inserter"].ingredients,{"advanced-circuit", 2})
  end
  if data.raw.recipe["purple-short-far-inserter"] then
    table.insert(data.raw["recipe"]["purple-short-far-inserter"].ingredients,{"advanced-circuit", 2})
  end
  if data.raw.recipe["purple-short-long-inserter"] then
    table.insert(data.raw["recipe"]["purple-short-long-inserter"].ingredients,{"advanced-circuit", 2})
  end
  if data.raw.recipe["purple-long-near-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-near-inserter"].ingredients,{"advanced-circuit", 2})
  end
  if data.raw.recipe["purple-long-short-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-short-inserter"].ingredients,{"advanced-circuit", 2})
  end
end

if data.raw.item["titanium-bearing"] then
  table.insert(data.raw["recipe"]["purple-inserter"].ingredients,{"titanium-bearing", 2})
  if data.raw.recipe["purple-near-inserter"] then
    table.insert(data.raw["recipe"]["purple-near-inserter"].ingredients,{"titanium-bearing", 2})
  end
  if data.raw.recipe["purple-far-inserter"] then
    table.insert(data.raw["recipe"]["purple-far-inserter"].ingredients,{"titanium-bearing", 2})
  end
  if data.raw.recipe["purple-long-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-inserter"].ingredients,{"titanium-bearing", 2})
  end
  if data.raw.recipe["purple-short-far-inserter"] then
    table.insert(data.raw["recipe"]["purple-short-far-inserter"].ingredients,{"titanium-bearing", 2})
  end
  if data.raw.recipe["purple-short-long-inserter"] then
    table.insert(data.raw["recipe"]["purple-short-long-inserter"].ingredients,{"titanium-bearing", 2})
  end
  if data.raw.recipe["purple-long-near-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-near-inserter"].ingredients,{"titanium-bearing", 2})
  end
  if data.raw.recipe["purple-long-short-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-short-inserter"].ingredients,{"titanium-bearing", 2})
  end
end

if data.raw.item["titanium-gear-wheel"] then
  table.insert(data.raw["recipe"]["purple-inserter"].ingredients,{"titanium-gear-wheel", 2})
  if data.raw.recipe["purple-near-inserter"] then
    table.insert(data.raw["recipe"]["purple-near-inserter"].ingredients,{"titanium-gear-wheel", 2})
  end
  if data.raw.recipe["purple-far-inserter"] then
    table.insert(data.raw["recipe"]["purple-far-inserter"].ingredients,{"titanium-gear-wheel", 2})
  end
  if data.raw.recipe["purple-long-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-inserter"].ingredients,{"titanium-gear-wheel", 2})
  end
  if data.raw.recipe["purple-short-far-inserter"] then
    table.insert(data.raw["recipe"]["purple-short-far-inserter"].ingredients,{"titanium-gear-wheel", 2})
  end
  if data.raw.recipe["purple-short-long-inserter"] then
    table.insert(data.raw["recipe"]["purple-short-long-inserter"].ingredients,{"titanium-gear-wheel", 2})
  end
  if data.raw.recipe["purple-long-near-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-near-inserter"].ingredients,{"titanium-gear-wheel", 2})
  end
  if data.raw.recipe["purple-long-short-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-short-inserter"].ingredients,{"titanium-gear-wheel", 2})
  end
else
  table.insert(data.raw["recipe"]["purple-inserter"].ingredients,{"iron-gear-wheel", 2})
  if data.raw.recipe["purple-near-inserter"] then
    table.insert(data.raw["recipe"]["purple-near-inserter"].ingredients,{"iron-gear-wheel", 2})
  end
  if data.raw.recipe["purple-far-inserter"] then
    table.insert(data.raw["recipe"]["purple-far-inserter"].ingredients,{"iron-gear-wheel", 2})
  end
  if data.raw.recipe["purple-long-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-inserter"].ingredients,{"iron-gear-wheel", 2})
  end
  if data.raw.recipe["purple-short-far-inserter"] then
    table.insert(data.raw["recipe"]["purple-short-far-inserter"].ingredients,{"iron-gear-wheel", 2})
  end
  if data.raw.recipe["purple-short-long-inserter"] then
    table.insert(data.raw["recipe"]["purple-short-long-inserter"].ingredients,{"iron-gear-wheel", 2})
  end
  if data.raw.recipe["purple-long-near-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-near-inserter"].ingredients,{"iron-gear-wheel", 2})
  end
  if data.raw.recipe["purple-long-short-inserter"] then
    table.insert(data.raw["recipe"]["purple-long-short-inserter"].ingredients,{"iron-gear-wheel", 2})
  end
end


